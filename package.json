{
  "name": "omnigpt-userscript",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node --check OmniGPT.user.js && node --test"
  }
}
