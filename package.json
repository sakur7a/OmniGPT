{
  "name": "omnigpt-userscript",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify:release": "node scripts/verify-release.mjs",
    "check": "npm run build && npm run verify:release && node --check OmniGPT.user.js && node --test"
  }
}
